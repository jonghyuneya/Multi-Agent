# AI 브리핑 시스템 문서

**Closing Briefing + Validation Agent 통합 시스템**

이 문서는 `closing_briefing`(AI 스크립트 생성)과 `validation_agent`(스크립트 검증) 시스템의 아키텍처, 데이터 스키마, 그리고 데이터 흐름을 설명합니다.

---

## 📋 목차

1. [시스템 개요](#시스템-개요)
2. [아키텍처 다이어그램](#아키텍처-다이어그램)
3. [Closing Briefing 시스템](#closing-briefing-시스템)
4. [Validation Agent 시스템](#validation-agent-시스템)
5. [데이터 스키마](#데이터-스키마)
6. [데이터 흐름](#데이터-흐름)
7. [소스 데이터](#소스-데이터)
8. [확장 가이드](#확장-가이드)

---

## 시스템 개요

### 목적
경제/금융 뉴스 기반의 **장마감 브리핑 스크립트**를 AI로 생성하고, 생성된 스크립트의 **사실 정확성**과 **대상 적합성**을 자동으로 검증하는 시스템입니다.

### 구성 요소

```
┌─────────────────────────────────────────────────────────────────────┐
│                        AI 브리핑 시스템                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────┐        ┌──────────────────────┐          │
│  │   closing_briefing   │───────▶│   validation_agent   │          │
│  │   (스크립트 생성)     │  출력   │   (스크립트 검증)     │          │
│  └──────────────────────┘        └──────────────────────┘          │
│            │                               │                        │
│            │                               │                        │
│            ▼                               ▼                        │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              te_calendar_scraper 출력 데이터                  │  │
│  │  (calendar, indicators, fomc_press_conferences, news)        │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 핵심 기술 스택

| 구성 요소 | 기술 |
|-----------|------|
| 스크립트 생성 | LangGraph, OpenAI GPT-4o |
| 도구 호출 | OpenAI Function Calling |
| 데이터 모델 | Pydantic |
| 데이터 소스 | CSV, JSON, DynamoDB |
| 검증 | LLM + Source Tool Binding |

---

## 아키텍처 다이어그램

```
                    ┌────────────────────────────────────────────────────────────────┐
                    │                   CLOSING BRIEFING PIPELINE                    │
                    └────────────────────────────────────────────────────────────────┘
                                                   │
         ┌─────────────────────────────────────────┼─────────────────────────────────────────┐
         │                                         │                                         │
         ▼                                         ▼                                         ▼
┌─────────────────┐                    ┌─────────────────────┐                   ┌──────────────────┐
│   DATA SOURCES  │                    │    GRAPH NODES      │                   │   OUTPUT         │
├─────────────────┤                    ├─────────────────────┤                   ├──────────────────┤
│                 │                    │                     │                   │                  │
│ te_calendar_    │───────────────────▶│ 1. load_sources     │                   │  script.md       │
│ scraper/output  │                    │    ↓                │                   │  (최종 스크립트)  │
│                 │                    │ 2. script_writer    │                   │                  │
│ ├─ calendar/    │     Tool Calls     │    (with tools)     │                   │  references      │
│ ├─ indicators/  │◀──────────────────▶│    ↓                │──────────────────▶│  (출처 목록)      │
│ ├─ fomc_press/  │                    │ 3. critic           │                   │                  │
│ └─ news (DB)    │                    │    (with tools)     │                   │  metadata.json   │
│                 │                    │    ↓                │                   │  (메타데이터)     │
└─────────────────┘                    │ 4. revision_writer  │                   │                  │
                                       │    ↓                │                   └──────────────────┘
                                       │    [END]            │
                                       └─────────────────────┘
                                                   │
                                                   │ Generated Script
                                                   ▼
                    ┌────────────────────────────────────────────────────────────────┐
                    │                   VALIDATION AGENT PIPELINE                    │
                    └────────────────────────────────────────────────────────────────┘
                                                   │
         ┌─────────────────────────────────────────┼─────────────────────────────────────────┐
         │                                         │                                         │
         ▼                                         ▼                                         ▼
┌─────────────────┐                    ┌─────────────────────┐                   ┌──────────────────┐
│  SOURCE TOOLS   │                    │    VALIDATORS       │                   │   RESULT         │
├─────────────────┤                    ├─────────────────────┤                   ├──────────────────┤
│                 │                    │                     │                   │                  │
│ TECalendar      │───────────────────▶│ 1. FactValidator    │                   │  ValidationResult│
│ SourceTool      │                    │    ↓                │                   │  ├─ claims       │
│                 │                    │ 2. AudienceValidator│──────────────────▶│  ├─ status       │
│ TEIndicators    │     search()       │    ↓                │                   │  ├─ audience_fit │
│ SourceTool      │◀──────────────────▶│ 3. CitationValidator│                   │  └─ summary      │
│                 │                    │                     │                   │                  │
│ FOMCSourceTool  │                    └─────────────────────┘                   └──────────────────┘
│                 │
│ NewsSourceTool  │
└─────────────────┘
```

---

## Closing Briefing 시스템

### 디렉토리 구조

```
closing_briefing/
├── __init__.py          # 패키지 exports
├── config.py            # 설정 (API keys, 모델, 온도)
├── models.py            # Pydantic 데이터 모델
├── tools.py             # LLM 도구 정의 및 실행
├── prompts.py           # 시스템 프롬프트
├── data_loader.py       # 데이터 로더
└── graph.py             # LangGraph 워크플로우
```

### 워크플로우 노드

| 노드 | 설명 | 도구 사용 |
|------|------|-----------|
| `load_sources` | CSV/JSON/DynamoDB에서 데이터 로드 | ❌ |
| `script_writer_with_tools` | 도구를 사용해 데이터 조회 후 스크립트 생성 | ✅ |
| `critic` | 도구를 사용해 사실 검증 및 피드백 생성 | ✅ |
| `revision_writer` | 피드백 기반 스크립트 수정 | ✅ |

### LLM 도구 (tools.py)

LLM이 호출할 수 있는 7개의 데이터 접근 도구:

```python
BRIEFING_TOOLS = [
    {
        "name": "get_macro_indicators",
        "description": "거시경제 지표 조회 (CPI, PMI, 금리 등)",
        "parameters": {
            "indicator_names": ["CPI YoY", "US 10Y Yield"]  # 선택적 필터
        }
    },
    {
        "name": "get_calendar_events",
        "description": "경제 캘린더 이벤트 조회",
        "parameters": {
            "importance": "high|medium|low|all",
            "days_ahead": 14
        }
    },
    {
        "name": "get_news_articles",
        "description": "뉴스 기사 조회",
        "parameters": {
            "category": "macro|company|sector|geopolitical|all",
            "limit": 10
        }
    },
    {
        "name": "get_earnings_results",
        "description": "기업 실적 조회",
        "parameters": {
            "sector": "Technology",
            "beat_only": true
        }
    },
    {
        "name": "get_fomc_events",
        "description": "FOMC 회의/기자회견 정보 조회",
        "parameters": {
            "include_snippets": true
        }
    },
    {
        "name": "get_market_summary",
        "description": "시장 요약 (지수, 섹터, VIX 등)",
        "parameters": {
            "include_sectors": true
        }
    },
    {
        "name": "search_data",
        "description": "전체 데이터 검색",
        "parameters": {
            "query": "Fed"  # 키워드 검색
        }
    }
]
```

### 상태 모델 (models.py)

```python
class ClosingBriefingState(BaseModel):
    """LangGraph 파이프라인의 상태 객체"""
    
    # 원본 데이터
    sources: Dict[str, Any]           # 로드된 소스 데이터
    
    # 추출된 정보
    extracted_facts: List[ExtractedFact]
    keywords: List[str]               # 오늘의 키워드 (1-3개)
    earnings_results: List[EarningsResult]
    news_items: List[NewsItem]
    upcoming_events: List[UpcomingEvent]
    macro_summary: List[MacroIndicator]
    
    # 스크립트 생성
    script_draft: Optional[str]       # 초안
    critic_feedback: Optional[CriticFeedback]
    script_revised: Optional[str]     # 수정본
    
    # 참조 추적
    references: List[Reference]       # 사용된 출처 목록
    
    # 반복 제어
    iterations: int = 0
    max_iterations: int = 1
    
    # 메타데이터
    briefing_date: Optional[str]
    error_message: Optional[str]
```

### Reference 모델

스크립트에서 사용된 모든 출처를 추적:

```python
class Reference(BaseModel):
    """스크립트에서 사용된 출처 참조"""
    source_type: str      # "macro_data", "calendar_events", "news_data", etc.
    source_file: str      # 데이터 출처 파일명
    quote: str            # 정확한 인용문/데이터 값
    provider: Optional[str]  # 뉴스 제공자 (Reuters, Bloomberg 등)
    date: Optional[str]   # 데이터 날짜
    
    def to_citation_tag(self) -> str:
        """[REF: source_type | "quote"] 형식으로 변환"""
        return f'[REF: {self.source_type} | "{self.quote}"]'
```

### Critic Feedback 모델

```python
class CriticFeedback(BaseModel):
    """Critic 에이전트의 피드백"""
    summary_evaluation: str           # 3-5문장 요약
    
    # 핵심 검증 항목
    hallucination_check: ChecklistItem   # 환각 검증
    timeliness_check: ChecklistItem      # 시의성 검증
    value_check: ChecklistItem           # 정보 가치 검증
    source_citation_check: ChecklistItem # 출처 명시 검증
    
    # 발견된 환각
    hallucinations_found: List[HallucinationItem]
    
    # 체크리스트 및 제안
    checklist: List[ChecklistItem]
    specific_suggestions: List[str]
    overall_quality: str  # "우수", "양호", "보통", "미흡", "심각"
    
    @property
    def needs_revision(self) -> bool:
        """수정이 필요한지 판단"""
```

---

## Validation Agent 시스템

### 디렉토리 구조

```
validation_agent/
├── __init__.py              # 패키지 exports
├── base.py                  # 기본 클래스 (SourceTool, Validator, ValidationAgent)
├── source_tools.py          # 소스 도구 구현
├── validators.py            # 검증기 구현
├── config.py                # 설정
├── main.py                  # CLI 진입점
└── examples/
    └── closing_briefing_validator.py  # closing_briefing용 설정
```

### 핵심 클래스

#### 1. SourceTool (추상 클래스)

```python
class SourceTool(ABC):
    """소스 데이터 도구의 기본 클래스"""
    
    @property
    @abstractmethod
    def source_type(self) -> str:
        """소스 타입 식별자 (예: 'calendar_events')"""
        pass
    
    @abstractmethod
    def load_sources(self, path: Path) -> None:
        """소스 데이터 로드"""
        pass
    
    @abstractmethod
    def search(self, query: str) -> List[Dict[str, Any]]:
        """쿼리로 데이터 검색"""
        pass
    
    @abstractmethod
    def validate_claim(self, claim: str, reference: str) -> SourceMatch:
        """주장을 소스 데이터와 대조하여 검증"""
        pass
    
    def get_tool_definition(self) -> Dict[str, Any]:
        """OpenAI Function Calling 스키마 반환"""
```

#### 2. Validator (추상 클래스)

```python
class Validator(ABC):
    """검증기의 기본 클래스"""
    
    @property
    @abstractmethod
    def validator_type(self) -> str:
        """검증기 타입 식별자"""
        pass
    
    @abstractmethod
    def validate(
        self,
        script: str,
        source_tools: Dict[str, SourceTool],
        **kwargs
    ) -> ValidationResult:
        """스크립트 검증 수행"""
        pass
```

#### 3. ValidationAgent (메인 클래스)

```python
class ValidationAgent:
    """소스 도구와 검증기를 조율하는 메인 에이전트"""
    
    def register_source_tool(self, tool: SourceTool) -> None:
        """소스 도구 등록"""
    
    def register_validator(self, validator: Validator) -> None:
        """검증기 등록"""
    
    def load_sources(self, source_paths: Dict[str, Path]) -> None:
        """소스 데이터 로드"""
    
    def validate(self, script: str, **kwargs) -> ValidationResult:
        """스크립트 검증 실행"""
```

### 소스 도구 구현

| 도구 클래스 | source_type | 데이터 소스 | Tool Name |
|------------|-------------|-------------|-----------|
| `TECalendarSourceTool` | `calendar_events` | calendar_*.csv | `search_calendar_events` |
| `TEIndicatorsSourceTool` | `macro_data` | indicators_*.csv | `search_macro_data` |
| `FOMCSourceTool` | `fomc_events` | fomc_press_conferences/*.pdf | `search_fomc_events` |
| `NewsSourceTool` | `news_data` | *.json | `search_news_data` |

### 검증기 구현

| 검증기 클래스 | validator_type | 역할 |
|--------------|----------------|------|
| `FactValidator` | `fact` | 사실 정확성 검증 (LLM + 도구 호출) |
| `AudienceValidator` | `audience` | 대상 적합성 평가 |
| `CitationValidator` | `citation` | 인용 완전성 확인 |

### ValidationResult 스키마

```python
@dataclass
class ValidationResult:
    """검증 결과"""
    script_id: str
    validated_at: datetime
    
    # 사실 검증 결과
    total_claims: int           # 전체 주장 수
    valid_claims: int           # 유효 주장 수
    invalid_claims: int         # 무효 주장 수
    not_found_claims: int       # 출처 미확인 주장 수
    source_matches: List[SourceMatch]  # 상세 매칭 결과
    
    # 대상 적합성
    audience_fitness: AudienceFitness  # excellent|good|fair|poor
    audience_feedback: str
    
    # 인용 검증
    citations_complete: bool
    missing_citations: List[str]
    
    # 전체 결과
    overall_valid: bool
    summary: str
    errors: List[str]
```

### SourceMatch 스키마

```python
@dataclass
class SourceMatch:
    """개별 주장의 검증 결과"""
    claim: str                    # 스크립트에서 추출한 주장
    source_type: str              # 소스 타입
    source_reference: str         # 인용된 출처
    source_data: Optional[Dict]   # 실제 찾은 소스 데이터
    status: ValidationStatus      # valid|partial|invalid|not_found|error
    confidence: float             # 0.0 ~ 1.0
    explanation: str              # 검증 결과 설명
    suggested_correction: Optional[str]  # 수정 제안
```

---

## 데이터 스키마

### 1. Calendar Events (calendar_*.csv)

```csv
datetime_utc,datetime_kst,title,category,impact,country,raw_time_text,source_url
2025-11-13T13:30:00Z,2025-11-13 22:30:00,CPI YoY,Inflation,high,US,Nov 13 08:30 AM,https://...
2025-11-14T08:00:00Z,2025-11-14 17:00:00,FOMC Meeting,Interest Rate,high,US,Nov 14 03:00 AM,https://...
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `datetime_utc` | ISO 8601 | UTC 시간 |
| `datetime_kst` | datetime | 한국 시간 |
| `title` | string | 이벤트 제목 |
| `category` | string | 카테고리 |
| `impact` | string | 중요도 (high/medium/low) |
| `country` | string | 국가 코드 |
| `raw_time_text` | string | 원본 시간 텍스트 |
| `source_url` | string | 출처 URL |

### 2. Macro Indicators (indicators_*.csv)

```csv
indicator_bucket,indicator_name,latest_value,unit,day_change,month_change,year_change,obs_date,source_url,raw_source_note
CPI,CPI YoY,3.0,percent,0.1,-0.2,0.5,2025-10-01,https://...,Bureau of Labor Statistics
UST,US 10Y Yield,4.08,percent,0.05,0.15,-0.25,2025-11-13,https://...,Federal Reserve
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `indicator_bucket` | string | 지표 그룹 (CPI, UST, ISM 등) |
| `indicator_name` | string | 지표명 |
| `latest_value` | float | 최신 값 |
| `unit` | string | 단위 |
| `day_change` | float | 일간 변동 |
| `month_change` | float | 월간 변동 |
| `year_change` | float | 연간 변동 |
| `obs_date` | date | 관측 날짜 |
| `source_url` | string | 출처 URL |
| `raw_source_note` | string | 원본 출처 메모 |

### 3. FOMC Press Conferences

```
fomc_press_conferences/
├── 2024_dec_17-18_press_conference.pdf
├── 2024_nov_06-07_press_conference.pdf
└── ...
```

파일명 형식: `{year}_{month}_{dates}_press_conference.pdf`

### 4. News Articles (JSON / DynamoDB)

```json
{
    "headline": "Fed Holds Rates Steady, Signals Future Cuts",
    "source": "Reuters",
    "published_date": "2025-11-13",
    "category": "macro",
    "summary": "The Federal Reserve maintained interest rates...",
    "market_impact": "positive",
    "tags": ["fed", "interest_rates", "monetary_policy"],
    "tickers": [],
    "url": "https://..."
}
```

### 5. Earnings Data (JSON)

```json
{
    "company_name": "NVIDIA",
    "ticker": "NVDA",
    "eps_actual": 0.81,
    "eps_estimate": 0.75,
    "revenue_actual": 35100000000,
    "revenue_estimate": 33000000000,
    "yoy_growth_pct": 94.0,
    "beat_or_miss": "beat",
    "sector": "Technology",
    "key_drivers": ["AI demand", "Data center growth"],
    "stock_reaction": "+5.2%"
}
```

### 6. Market Summary (JSON)

```json
{
    "date": "2025-11-13",
    "indices": {
        "SPX": {"close": 5998.23, "change_pct": 0.75},
        "NASDAQ": {"close": 19286.78, "change_pct": 1.12},
        "DJI": {"close": 44234.56, "change_pct": 0.45}
    },
    "vix": 15.23,
    "dollar_index": 104.56,
    "us_10y_yield": 4.08,
    "crude_oil_wti": 72.45,
    "gold": 2685.30,
    "sectors": {
        "Technology": 1.25,
        "Healthcare": 0.45,
        "Financials": 0.78
    }
}
```

---

## 데이터 흐름

### Closing Briefing 데이터 흐름

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                            CLOSING BRIEFING DATA FLOW                           │
└─────────────────────────────────────────────────────────────────────────────────┘

1. DATA LOADING
   ┌──────────────────┐
   │ te_calendar_     │
   │ scraper/output   │
   │ ├─ calendar/     │──────────┐
   │ ├─ indicators/   │          │
   │ ├─ fomc_press/   │          ▼
   │ └─ ...           │   ┌─────────────────┐
   └──────────────────┘   │   load_sources  │
                          │      node       │
   ┌──────────────────┐   │                 │
   │   DynamoDB       │──▶│   sources: {    │
   │   (news data)    │   │     macro_data  │
   └──────────────────┘   │     calendar    │
                          │     news        │
                          │     earnings    │
                          │     fomc        │
                          │   }             │
                          └────────┬────────┘
                                   │
                                   ▼
2. SCRIPT GENERATION (WITH TOOL CALLS)
   ┌─────────────────────────────────────────────────────────────────┐
   │                    script_writer_with_tools                     │
   │                                                                 │
   │  LLM Request ─────▶ "get_macro_indicators(['CPI', 'PMI'])"     │
   │       │                        │                                │
   │       │                        ▼                                │
   │       │            ┌─────────────────────┐                      │
   │       │            │  DataToolExecutor   │                      │
   │       │            │  - _get_macro_...() │                      │
   │       │            │  - _get_calendar()  │                      │
   │       │            │  - _get_news()      │                      │
   │       │            └──────────┬──────────┘                      │
   │       │                       │                                 │
   │       │                       ▼                                 │
   │       │            {                                            │
   │       │              "data": [...],                             │
   │       │              "references": [                            │
   │       │                {                                        │
   │       │                  "source_type": "macro_data",           │
   │       │                  "quote": "CPI YoY: 3.0%",              │
   │       │                  "source_file": "indicators_US.csv"    │
   │       │                }                                        │
   │       │              ]                                          │
   │       │            }                                            │
   │       │                       │                                 │
   │       ◀───────────────────────┘                                 │
   │                                                                 │
   │  Output: script_draft + references                              │
   └─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
3. CRITIC VALIDATION (WITH TOOL CALLS)
   ┌─────────────────────────────────────────────────────────────────┐
   │                         critic node                             │
   │                                                                 │
   │  Verify each claim ──▶ Call tools to check source data         │
   │                                                                 │
   │  Output: CriticFeedback {                                       │
   │    hallucination_check,                                         │
   │    timeliness_check,                                            │
   │    hallucinations_found: [],                                    │
   │    specific_suggestions: []                                     │
   │  }                                                              │
   └─────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
4. CONDITIONAL REVISION
   ┌────────────────────┐      ┌────────────────────┐
   │ needs_revision?    │──Yes─▶│ revision_writer   │
   │ (hallucinations,   │      │ (도구로 재검증)      │
   │  quality issues)   │      └────────┬───────────┘
   └────────┬───────────┘               │
            │ No                        │
            ▼                           ▼
   ┌────────────────────┐      ┌────────────────────┐
   │       END          │◀─────│    critic again?   │
   │  (final script)    │      │  (max iterations?) │
   └────────────────────┘      └────────────────────┘

5. OUTPUT
   ┌─────────────────────────────────────────────────────────────────┐
   │  closing_briefing_20251113_143025.md                            │
   │  ├─ 키워드: AI, 반도체, 금리                                      │
   │  ├─ 스크립트 본문 (Host + Analyst 대화)                           │
   │  │   각 문장에 [REF: source_type | "quote"] 태그 포함            │
   │  └─ 참고 자료 (References) 섹션                                   │
   │                                                                 │
   │  closing_briefing_20251113_143025_meta.json                     │
   │  ├─ references: [...]                                           │
   │  ├─ critic_feedback: {...}                                      │
   │  └─ iterations: 1                                               │
   └─────────────────────────────────────────────────────────────────┘
```

### Validation Agent 데이터 흐름

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                          VALIDATION AGENT DATA FLOW                             │
└─────────────────────────────────────────────────────────────────────────────────┘

1. SETUP
   ┌──────────────────────────────────────────────────────────────────────────┐
   │  agent = ValidationAgent()                                               │
   │                                                                          │
   │  # Register source tools                                                 │
   │  agent.register_source_tool(TECalendarSourceTool())                      │
   │  agent.register_source_tool(TEIndicatorsSourceTool())                    │
   │  agent.register_source_tool(FOMCSourceTool())                            │
   │  agent.register_source_tool(NewsSourceTool())                            │
   │                                                                          │
   │  # Register validators                                                   │
   │  agent.register_validator(FactValidator())                               │
   │  agent.register_validator(AudienceValidator())                           │
   │  agent.register_validator(CitationValidator())                           │
   │                                                                          │
   │  # Load sources                                                          │
   │  agent.load_sources({                                                    │
   │      "calendar_events": Path(".../output/calendar"),                     │
   │      "macro_data": Path(".../output/indicators"),                        │
   │      "fomc_events": Path(".../output/fomc_press_conferences"),           │
   │  })                                                                      │
   └──────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
2. VALIDATION EXECUTION
   ┌──────────────────────────────────────────────────────────────────────────┐
   │  result = agent.validate(script_text)                                    │
   │                                                                          │
   │  For each validator:                                                     │
   │  ┌────────────────────────────────────────────────────────────────────┐  │
   │  │ FactValidator                                                      │  │
   │  │                                                                    │  │
   │  │  1. Extract claims from script                                     │  │
   │  │  2. For each claim:                                                │  │
   │  │     LLM ──▶ "search_macro_data('CPI')" ──▶ tool.search()           │  │
   │  │          ◀── [matching records] ◀──────────────────────            │  │
   │  │  3. Compare claim vs source data                                   │  │
   │  │  4. Generate SourceMatch results                                   │  │
   │  └────────────────────────────────────────────────────────────────────┘  │
   │  ┌────────────────────────────────────────────────────────────────────┐  │
   │  │ AudienceValidator                                                  │  │
   │  │                                                                    │  │
   │  │  Evaluate: relevance, accessibility, actionability, tone, structure│  │
   │  │  Output: AudienceFitness + feedback                                │  │
   │  └────────────────────────────────────────────────────────────────────┘  │
   │  ┌────────────────────────────────────────────────────────────────────┐  │
   │  │ CitationValidator                                                  │  │
   │  │                                                                    │  │
   │  │  Check: [REF: ...] tags for all factual claims                     │  │
   │  │  Output: citations_complete + missing_citations                    │  │
   │  └────────────────────────────────────────────────────────────────────┘  │
   └──────────────────────────────────────────────────────────────────────────┘
                                       │
                                       ▼
3. RESULT AGGREGATION
   ┌──────────────────────────────────────────────────────────────────────────┐
   │  ValidationResult {                                                      │
   │      script_id: "closing_briefing_20251113",                             │
   │      validated_at: "2025-11-13T14:35:00",                                │
   │                                                                          │
   │      total_claims: 15,                                                   │
   │      valid_claims: 13,                                                   │
   │      invalid_claims: 1,                                                  │
   │      not_found_claims: 1,                                                │
   │                                                                          │
   │      source_matches: [                                                   │
   │          SourceMatch(claim="CPI 3.0%", status=VALID, confidence=0.95),   │
   │          SourceMatch(claim="Fed 금리동결", status=VALID, confidence=0.9),│
   │          SourceMatch(claim="...", status=INVALID, suggestion="..."),     │
   │      ],                                                                  │
   │                                                                          │
   │      audience_fitness: "good",                                           │
   │      audience_feedback: "✅ 장점: 전문적 분석...",                         │
   │                                                                          │
   │      citations_complete: true,                                           │
   │      missing_citations: [],                                              │
   │                                                                          │
   │      overall_valid: false,  // 1 invalid claim                           │
   │      summary: "사실 검증: 13/15 (86.7%) 일치\n⚠️ 불일치: 1건\n..."        │
   │  }                                                                       │
   └──────────────────────────────────────────────────────────────────────────┘
```

---

## 소스 데이터

### te_calendar_scraper 출력 구조

```
te_calendar_scraper/output/
├── calendar/
│   └── calendar_US_20251112_20251126.csv   # 경제 이벤트 일정
├── indicators/
│   ├── indicators_US_20251112.csv          # 거시경제 지표
│   └── indicators_US_20251113.csv
├── fomc_press_conferences/
│   ├── 2024_dec_17-18_press_conference.pdf
│   ├── 2024_nov_06-07_press_conference.pdf
│   └── ...
└── speeches_transcripts/
    └── ...                                  # 연준 연설문
```

### 데이터 소스 매핑

| 용도 | Closing Briefing 도구 | Validation Agent 도구 | 데이터 파일 |
|------|----------------------|----------------------|-------------|
| 경제 일정 | `get_calendar_events` | `search_calendar_events` | `calendar/calendar_*.csv` |
| 거시 지표 | `get_macro_indicators` | `search_macro_data` | `indicators/indicators_*.csv` |
| FOMC | `get_fomc_events` | `search_fomc_events` | `fomc_press_conferences/*.pdf` |
| 뉴스 | `get_news_articles` | `search_news_data` | DynamoDB / JSON |
| 실적 | `get_earnings_results` | - | `earnings_data.json` |
| 시장요약 | `get_market_summary` | - | `market_summary.json` |

---

## 확장 가이드

### 새 소스 도구 추가 (Validation Agent)

```python
from validation_agent.base import SourceTool, SourceMatch, ValidationStatus
from pathlib import Path
from typing import Any, Dict, List

class MyCustomSourceTool(SourceTool):
    """사용자 정의 소스 도구"""
    
    def __init__(self):
        self._data = []
        self._loaded = False
    
    @property
    def source_type(self) -> str:
        return "my_custom_source"
    
    def load_sources(self, path: Path) -> None:
        # CSV, JSON, DB 등에서 데이터 로드
        import json
        with open(path) as f:
            self._data = json.load(f)
        self._loaded = True
    
    def search(self, query: str) -> List[Dict[str, Any]]:
        # 쿼리로 데이터 검색
        results = []
        for item in self._data:
            if query.lower() in str(item).lower():
                results.append(item)
        return results
    
    def validate_claim(self, claim: str, reference: str) -> SourceMatch:
        # 주장 검증
        matches = self.search(reference)
        
        if not matches:
            return SourceMatch(
                claim=claim,
                source_type=self.source_type,
                source_reference=reference,
                status=ValidationStatus.NOT_FOUND,
                explanation="데이터를 찾을 수 없음"
            )
        
        return SourceMatch(
            claim=claim,
            source_type=self.source_type,
            source_reference=reference,
            source_data=matches[0],
            status=ValidationStatus.VALID,
            confidence=0.9,
            explanation="검증 완료"
        )
    
    def get_tool_definition(self) -> Dict[str, Any]:
        return {
            "type": "function",
            "function": {
                "name": "search_my_custom_source",
                "description": "내 사용자 정의 소스에서 데이터 검색",
                "parameters": {
                    "type": "object",
                    "properties": {
                        "query": {
                            "type": "string",
                            "description": "검색 쿼리"
                        }
                    },
                    "required": ["query"]
                }
            }
        }

# 등록
from validation_agent import ValidationAgent
agent = ValidationAgent()
agent.register_source_tool(MyCustomSourceTool())
```

### 새 LLM 도구 추가 (Closing Briefing)

`tools.py`에 도구 정의 추가:

```python
# BRIEFING_TOOLS 리스트에 추가
{
    "type": "function",
    "function": {
        "name": "get_my_custom_data",
        "description": "사용자 정의 데이터 조회",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "검색 쿼리"
                }
            },
            "required": ["query"]
        }
    }
}

# DataToolExecutor 클래스에 메서드 추가
def _get_my_custom_data(self, args: Dict) -> Dict[str, Any]:
    """사용자 정의 데이터 조회"""
    query = args.get('query', '')
    custom_data = self.sources.get('my_custom_data', [])
    
    results = []
    references = []
    
    for item in custom_data:
        if query.lower() in str(item).lower():
            ref = self._create_reference(
                source_type="my_custom_data",
                source_file="custom_data.json",
                quote=str(item),
            )
            results.append({**item, "reference": ref})
            references.append(ref)
    
    return {"data": results, "references": references}

# execute_tool의 tool_map에 추가
tool_map = {
    ...
    "get_my_custom_data": self._get_my_custom_data,
}
```

---

## 사용 예시

### Closing Briefing 실행

```python
from closing_briefing.graph import build_graph
from closing_briefing.models import ClosingBriefingState

# 그래프 빌드
graph = build_graph()

# 초기 상태 설정
initial_state = ClosingBriefingState(
    sources={
        "_source_path": "/home/jhkim/te_calendar_scraper/output",
        "_load_news_from_dynamodb": True,
    },
    max_iterations=2,
)

# 실행
final_state = graph.invoke(initial_state)

# 결과 출력
print(final_state["script_revised"] or final_state["script_draft"])
```

### Validation Agent 실행

```python
from validation_agent.examples import create_closing_briefing_validator

# 검증기 생성
validator = create_closing_briefing_validator(
    te_calendar_output_path="/home/jhkim/te_calendar_scraper/output"
)

# 스크립트 검증
with open("closing_script.txt") as f:
    script = f.read()

result = validator.validate(script)

# 결과 확인
print(result.summary)
if not result.overall_valid:
    for match in result.source_matches:
        if match.status.value != "valid":
            print(f"Issue: {match.claim[:50]}... - {match.explanation}")
```

### CLI 사용

```bash
# Closing Briefing 실행
python -m closing_briefing.main --source-path /path/to/data --output /path/to/output

# Validation Agent 실행
python -m validation_agent.main --script closing_script.txt --output-json
```

### 구조화된 브리핑 스크립트 검증

`validation_agent`는 아래와 같은 구조화된 JSON 스크립트도 검증할 수 있습니다:

```python
from validation_agent import validate_briefing_script
from pathlib import Path

# 스크립트 JSON 파일 읽기
with open("briefing_script.json") as f:
    script_json = f.read()

# 검증 실행
result = validate_briefing_script(
    script_json,
    articles_path=Path("articles.json"),  # 뉴스 기사 데이터
    events_path=Path("events.json"),      # 경제 이벤트 데이터
    validate_content=True,                 # LLM으로 내용 검증
)

print(result.summary)
```

---

## 구조화된 브리핑 스크립트 스키마

### 전체 스크립트 구조

```json
{
    "date": "20251222",
    "nutshell": "기록적 금값과 AI 랠리 속 선택적 강세장",
    "user_tickers": [],
    "chapter": [
        {"name": "opening", "start_id": 0, "end_id": 5},
        {"name": "theme", "start_id": 6, "end_id": 29},
        {"name": "closing", "start_id": 30, "end_id": 38}
    ],
    "scripts": [...]
}
```

### 스크립트 항목 (scripts)

```json
{
    "id": 1,
    "speaker": "해설자",
    "text": "금 선물은 온스당 4천4백달러 중반의 사상 최고가를 기록했습니다...",
    "sources": [...],
    "time": [14871, 60602]
}
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `id` | int | 스크립트 항목 고유 ID |
| `speaker` | string | 발화자 ("진행자" 또는 "해설자") |
| `text` | string | 스크립트 내용 (한국어) |
| `sources` | array | 출처 목록 |
| `time` | [int, int] | 시작/종료 시간 (밀리초) |

### 출처 유형 (sources)

#### 1. Article (뉴스 기사)

```json
{
    "type": "article",
    "pk": "id#9e4894ca63cd8d66",
    "title": "Gold Rises to Record High on Rate-Cut Bets and Venezuela Tension"
}
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `type` | "article" | 출처 유형 |
| `pk` | string | DynamoDB 기본 키 (id#xxx 형식) |
| `title` | string | 기사 제목 |

#### 2. Chart (시장 데이터)

```json
{
    "type": "chart",
    "ticker": "^GSPC",
    "start_date": "2025-12-19",
    "end_date": "2025-12-22"
}
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `type` | "chart" | 출처 유형 |
| `ticker` | string | 종목 코드 (^GSPC, GC=F, AAPL 등) |
| `start_date` | string | 차트 시작 날짜 (YYYY-MM-DD) |
| `end_date` | string | 차트 종료 날짜 (YYYY-MM-DD) |

#### 3. Event (경제 이벤트)

```json
{
    "type": "event",
    "id": "387585",
    "title": "gdp growth rate qoq",
    "date": "2025-12-23"
}
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `type` | "event" | 출처 유형 |
| `id` | string | 이벤트 고유 ID |
| `title` | string | 이벤트 제목 |
| `date` | string | 이벤트 날짜 (YYYY-MM-DD) |

### 검증 도구

구조화된 스크립트 검증을 위한 추가 소스 도구:

| 도구 클래스 | source_type | Tool Name | 용도 |
|------------|-------------|-----------|------|
| `ArticleSourceTool` | `article` | `search_article` | pk 또는 title로 기사 검색 |
| `EventSourceTool` | `event` | `search_event` | id 또는 title로 이벤트 검색 |
| `BriefingScriptSourceTool` | `briefing_script` | `search_briefing_sources` | 모든 출처 통합 검색 |

### 검증 과정

1. **출처 존재 검증** (`ScriptSourceValidator`):
   - 모든 article의 pk가 실제로 존재하는지 확인
   - 모든 event의 id가 실제로 존재하는지 확인
   - chart는 ticker 유효성만 확인 (가격 검증은 별도 API 필요)

2. **내용 일치 검증** (`ScriptContentValidator`):
   - LLM을 사용하여 스크립트 텍스트가 출처 내용과 일치하는지 확인
   - 과장, 왜곡, 맥락 오류 감지
   - 대상 청중 적합성 평가

---

## 라이선스

MIT

